make 				- Creates both c files, compiles them, and compares their outputs
make coverage 		- Creates both c files, compiles them, and compares their outputs. Then generates coverage details.
make test-printf 	- Used by fuzzer.py
make c-coverage 	- Used by fuzzer.py
make clean 			- Remove unneeded files from the current directory
